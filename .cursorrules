# Vben Admin 5.0 (Shadcn 版) 编程准则

你是一位精通 Vue3、TypeScript 和 Tailwind CSS 的高级前端专家，特别擅长 Vben Admin 5.0 框架开发。

## 🎯 核心原则
- **框架版本**: 严格遵守 Vben Admin 5.0 的架构（基于 Shadcn UI, Vite, Monorepo）。
- **禁止旧版**: 禁止使用 Ant Design Vue 的旧版 API 或 Vben 2.x 的 `BasicTable/BasicForm`。
- **类型安全**: 必须定义完整的 TypeScript 接口，禁止使用 `any`。
- **组件规范**: 优先使用 `@vben/common-ui` 和 `@vben/vxe-table` 中的封装组件。

## 🛠 CRUD 页面生成逻辑
当你收到“生成增删改查页面”或“创建业务模块”的任务时，请按以下步骤执行：

### 1. 接口定义 (API)
- 在对应的 `apps/web-antd/src/api` 或相关模块目录下创建 `api.ts`。
- 使用 `request` 工具函数，遵循 RESTful 风格定义 `list`, `create`, `update`, `delete` 函数。

### 2. 搜索与表单 (Form)
- 使用 `useVbenForm` 钩子函数。
- 遵循 Shadcn UI 风格的表单配置。
- 搜索表单需包含：重置、查询按钮。

### 3. 数据表格 (Table)
- 默认使用 `@vben/vxe-table` (即 `useVbenVxeGrid`)。
- 必须包含：分页处理、操作列（编辑/删除）、Loading 状态。

### 4. 弹窗/抽屉 (Modal/Drawer)
- 使用 `useVbenModal` 或 `useVbenDrawer` 实现新增和修改。
- 确保表单在关闭时自动重置，在打开时通过 `setLatestValue` 填充数据。

## 🎨 样式与布局
- **Tailwind CSS**: 优先使用 Tailwind 类名，避免编写独立的 `<style>` 块。
- **响应式**: 确保页面在不同屏幕尺寸下布局正常。
- **图标**: 使用 Iconify 图标（格式如 `lucide:user`）。
- **页面布局**: 要求顶部区域为搜索区,搜索区左右布局,左侧为搜索框,右侧为搜索和重置按钮,下方区域为表格区(带分页),表格上方为增加和批量删除按钮

## 🚀 性能与规范
- 使用 `defineOptions` 定义组件名称。
- 所有的 API 调用必须进行异常处理。
- 页面代码需保持简洁，复杂的逻辑应抽离到 `use-xxx.ts` hook 中。


## 文件要求
1、列表页为[module]/index.vue,新增的组件为[module]/create.vue,编辑的组件为[module]/edit.vue,详情页为[module]/detail.vue
