## 1. 侧边栏数据是如何引入的？

目前项目中侧边栏的数据主要是通过 路由配置（Route Modules） 动态生成的。

在 Vben Admin v5 中，菜单数据默认由前端路由模块驱动：

- 自动化加载：在 `apps/web-antdv-next/src/router/routes/index.ts` 中，通过 `import.meta.glob` 自动扫描 `src/router/routes/modules/` 目录下所有的 `.ts` 文件。
- 路由转换菜单：生成的路由表会被 accessStore 处理，根据路由中的 meta 配置（如 title, icon, hideInMenu 等）自动生成侧边栏菜单。
- 权限模式：当前的默认模式是 frontend（由前端定义路由）。如果在 `src/preferences.ts` 中将 accessMode 改为 backend，则会通过 `src/api/core/menu.ts` 调用的 `/menu/all `接口获取数据。

## 2. 如何只保留“工作台”菜单？

要实现只保留“工作台”并删除其他菜单，你需要执行以下三个步骤：

- 第一步：删除或清空不必要的路由模块删除`apps/web-antdv-next/src/router/routes/modules/`下除了 `dashboard.ts `以外的其他文件（或将其内容清空/注释掉）：
  - demos.ts (演示菜单)
  - vben.ts (项目相关及关于页菜单)

- 第二步：修改 dashboard.ts 只保留工作台修改 `apps/web-antdv-next/src/router/routes/modules/dashboard.ts`，将其简化为只有一个菜单项。如果你希望“工作台”直接显示在第一级（不需要点击折叠菜单），可以将其平铺：

```typescript
// apps/web-antdv-next/src/router/routes/modules/dashboard.ts
import type { RouteRecordRaw } from 'vue-router';
import { $t } from '#/locales';

const routes: RouteRecordRaw[] = [
  {
    meta: {
      icon: 'carbon:workspace',
      title: $t('page.dashboard.workspace'),
    },
    name: 'Workspace',
    path: '/workspace',
    component: () => import('#/views/dashboard/workspace/index.vue'),
  },
];

export default routes;
```

- 更新默认首页路径由于你删除了“分析页” `(/analytics)`，需要将系统默认跳转的首页改为“工作台” `(/workspace)`。 修改 `apps/web-antdv-next/src/preferences.ts`：

```typescript
export const overridesPreferences = defineOverridesPreferences({
  app: {
    name: import.meta.env.VITE_APP_TITLE,
    // 将默认登录后的跳转地址改为工作台
    defaultHomePath: '/workspace',
  },
});
```

- 菜单隐藏:可以在路由配置中设置`meta: { hideInMenu: true }`

## 在package中引入js-cookie

1、在根目录下执行

```bash
pnpm add js-cookie -D --filter @vben/utils
```

2、在`@vben/utils/src/helpers/`中增加cookie.ts文件

```typescript
import Cookies from 'js-cookie';
// 完成对cookie的封装后
```

3、在`@vben/utils/src/index.ts`中导出cookie.ts文件

```typescript
export * from './helpers/cookie';
```

4、在`apps/web-antdv-next/src/store/auth.ts`中引入cookie.ts文件

```typescript
import { setCookie } from '@vben/utils';
```

## AI提示题

```text
“我想在项目中增加一个 Shadcn 的 [组件名]。请参考 packages/@vben-core/shadcn-ui 现有的组件结构，在对应目录下为我创建一个符合 Vben 5.0 规范的组件版本。”
```

## icon文档

[lucide](https://lucide.dev/icons/)

### icon引入

在`packages/@core/base/icons/src/lucide.ts`中导出需要的icon,具体的icon名称参考[lucide](https://lucide.dev/icons/)

## 无界子应用：向基座拉取 Token / 明暗模式 / 内置主题

基座在 `bootstrap` 中注册了 `setupWujieHostBridge()`，事件名定义在 `apps/web-antdv-next/src/wujie-config/event.ts`。

**子应用主动请求（回调方式，与无界文档一致）：**

```ts
const bus = window.$wujie?.bus;
// 一次拉取完整状态（推荐）：state = { token, colorMode: 'dark'|'light', builtinType }
bus?.$emit('hostBridge:requestHostState', (state) => {
  console.log(state.token, state.colorMode, state.builtinType);
});
// 或分别请求
bus?.$emit('hostBridge:requestToken', (token: string) => {});
bus?.$emit('hostBridge:requestColorMode', (mode: 'dark' | 'light') => {});
bus?.$emit('hostBridge:requestBuiltinTheme', (builtinType: string) => {});
```

**基座推送：** Token、主题模式或内置主题变更时，基座会 `emit('hostBridge:hostStatePush', state)`，子应用可 `bus.$on('hostBridge:hostStatePush', handler)` 保持同步。

## 路由模式

```text(apps/web-antdv-next/src/preferences.ts)
// 权限模式：当前的默认模式是 frontend（由前端定义路由）。
// 如果需要后端返回路由，请将该值改为 backend。
accessMode: 'frontend',
```

## Tailwind
**Tailwind 的默认单位是 1unit = 4px, mx-6表示水平方向左右边距为4*6=24px, 左ml-6, 右mr-6**

##  菜单加载与权限逻辑
- **菜单获取**: 登录之后（如果是多租户用户，在选完租户点击确定刷新 Token 之后），必须统一通过接口 `/de-base-system/external/private/mine/feature` 获取左侧菜单树。
- accessMode: 默认是frontend(前端渲染),可以选backend(后端渲染),mixed(混合)

## 状态管理与存储逻辑 (Cookie & Store)
- **Token 管理**: Token 必须同时存在 Pinia Store 和 Cookie 中。
  - 使用 `@vben/utils` 中封装的 `setAuthToken` (基于 `js-cookie`) 进行操作。
- **用户信息**: 登录逻辑应在 `packages/effects` 相关的 Store 中处理。
- **大数据处理**: 接口返回的长整型 ID 必须通过 `json-bigint` 处理，确保在 `packages/@vben/request` 的拦截器中已配置数据转换。
