## 1. 侧边栏数据是如何引入的？

目前项目中侧边栏的数据主要是通过 路由配置（Route Modules） 动态生成的。

在 Vben Admin v5 中，菜单数据默认由前端路由模块驱动：

- 自动化加载：在 `apps/web-antdv-next/src/router/routes/index.ts`
 中，通过 `import.meta.glob` 自动扫描 `src/router/routes/modules/` 目录下所有的 `.ts`
 文件。
- 路由转换菜单：生成的路由表会被 accessStore 处理，根据路由中的 meta 配置（如 title, icon, hideInMenu 等）自动生成侧边栏菜单。
- 权限模式：当前的默认模式是 frontend（由前端定义路由）。如果在 `src/preferences.ts`
 中将 accessMode 改为 backend，则会通过 
`src/api/core/menu.ts`
 调用的 `/menu/all `接口获取数据。

## 2. 如何只保留“工作台”菜单？
要实现只保留“工作台”并删除其他菜单，你需要执行以下三个步骤：

- 第一步：删除或清空不必要的路由模块
删除` apps/web-antdv-next/src/router/routes/modules/ `下除了 `dashboard.ts `以外的其他文件（或将其内容清空/注释掉）：

  - demos.ts (演示菜单)
  - vben.ts (项目相关及关于页菜单)
- 第二步：修改 dashboard.ts 只保留工作台
修改 `apps/web-antdv-next/src/router/routes/modules/dashboard.ts`，将其简化为只有一个菜单项。如果你希望“工作台”直接显示在第一级（不需要点击折叠菜单），可以将其平铺：

```typescript
// apps/web-antdv-next/src/router/routes/modules/dashboard.ts
import type { RouteRecordRaw } from 'vue-router';
import { $t } from '#/locales';

const routes: RouteRecordRaw[] = [
  {
    meta: {
      icon: 'carbon:workspace',
      title: $t('page.dashboard.workspace'),
    },
    name: 'Workspace',
    path: '/workspace',
    component: () => import('#/views/dashboard/workspace/index.vue'),
  },
];

export default routes;

```

- 更新默认首页路径
由于你删除了“分析页” `(/analytics)`，需要将系统默认跳转的首页改为“工作台” `(/workspace)`。 修改 `apps/web-antdv-next/src/preferences.ts`：
```typescript
export const overridesPreferences = defineOverridesPreferences({
  app: {
    name: import.meta.env.VITE_APP_TITLE,
    // 将默认登录后的跳转地址改为工作台
    defaultHomePath: '/workspace',
  },
});

```
- 菜单隐藏:可以在路由配置中设置`meta: { hideInMenu: true }`

